<div class="col-md-8">
<h1>Búsqueda de paseadores</h1>
</div>

<div class="container"> 
  <div style='width: 600px;'>
    <div id="map" style='width: 650px; height: 400px;'></div>
  </div>
</div>
<script> 
handler = Gmaps.build('Google');
handler.buildMap({
        provider: {
        center: new google.maps.LatLng({lat: <%= current_user.latitude %> , lng: <%= current_user.longitude %> }),
        zoom: 10
      },
      internal: {
        id: 'map'
      }
    },
 function(){
    markers = handler.addMarkers(<%=raw @hash2.to_json %>);
    
  }
);

</script>